# Single-GPU (RTX 4090, 24GB, sm89) overrides for OSCAR eval scripts.
# Upstream defaults assume 4-GPU TP=4 production; these are the working values.
MEM_FRAC=0.72
# Paper-knee KV windows (defaults, stated explicitly for the record):
SGLANG_MIXED_KV_PREFIX_TOKENS=64
SGLANG_MIXED_KV_RECENT_TOKENS=256
